<?xml version="1.0" encoding="utf-8"?>
<!-- Upstream: SupportWearDemos res/layout/switch_demo.xml is a <merge> whose
     Switch carries style="@style/Widget.Wear.RoundSwitch" from the wear
     library res. CDROID's wear module does not ship that style and its
     inflater has no <merge> root here, so this is the same centered Switch
     on a plain FrameLayout, unstyled. -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>
</FrameLayout>
