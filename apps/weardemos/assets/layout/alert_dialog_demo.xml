<?xml version="1.0" encoding="utf-8"?>
<!-- Upstream: SupportWearDemos res/layout/alert_dialog_demo.xml (app:
     boxedEdges resolves via the widgetex.apk shared lib; see rv_demo.xml). -->
<androidx.wear.widget.BoxInsetLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:boxedEdges="all">
        <Button
            android:id="@+id/v7_dialog_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Show V7 dialog"/>
        <Button
            android:id="@+id/framework_dialog_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Show Framework dialog"/>
    </LinearLayout>
</androidx.wear.widget.BoxInsetLayout>
