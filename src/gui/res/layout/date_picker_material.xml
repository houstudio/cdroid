<?xml version="1.0" encoding="utf-8"?>
<!-- Container inflated by DatePickerCalendarDelegate. Header (year + month-day) on top,
     ViewAnimator switches between the month grid (DayPickerView) and the year list
     (YearPickerView). Colors are literal so the header is readable on a dark surface. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/date_picker_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/date_picker_header_year"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFECEFF2"
            android:textSize="22sp"
            android:textStyle="bold"
            android:focusable="true"
            android:clickable="true" />

        <TextView
            android:id="@+id/date_picker_header_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="#FF9BA6B2"
            android:textSize="16sp"
            android:focusable="true"
            android:clickable="true" />
    </LinearLayout>

    <ViewAnimator
        android:id="@+id/animator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <DayPickerView
            android:id="@+id/date_picker_day_picker"
            android:layout_width="match_parent"
            android:layout_height="240dp" />

        <YearPickerView
            android:id="@+id/date_picker_year_picker"
            android:layout_width="match_parent"
            android:layout_height="200dp" />
    </ViewAnimator>

</LinearLayout>
